import spotify
from env import user
import random

def get_albums(user):
    return albums

def random_album(user):
    albums = get_albums(user)
    album = random.choice(albums)
    return album_title